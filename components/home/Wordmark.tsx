/** The wordmark on a cream slip, so it reads the same over the table or any sheet. */
export default function Wordmark() {
  return (
    <p className="slip inline-block px-3 pt-2 pb-2.5 font-serif italic text-[28px] leading-none">
      Lakes of Bendakaluru
    </p>
  );
}
